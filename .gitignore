# ---- OS ----
.DS_Store
Thumbs.db
ehthumbs.db

# ---- Editors / IDEs ----
.idea/
*.iml
*.swp

# ---- Logs ----
*.log

# ---- Secrets (NEVER commit) ----
.env
.env.*
**/serviceAccount*.json
**/*firebase-adminsdk*.json

# Note: the Flutter app in mobile/ has its own .gitignore for build artifacts
# (build/, .dart_tool/, etc.). The backend will add its own when it lands.
#
# firebase_options.dart IS committed on purpose: the web apiKey is not a secret —
# it identifies the project to Firebase and is meant to ship in client code.
